'use strict';

angular.module('bnePaymentsFrontApp')
  .controller('MainCtrl', function ($scope, $http) {

/*    $scope.account = "";

    $scope.updateAccounts = function(typed) {
      $http({method: 'GET', url: 'http://localhost:4567/api/accounts?query=' + typed}).
      success(function(data, status, headers, config) {
        var accounts = [];
        $.each(data.response.docs, function(i, val) {
          accounts.push(val.name);
        });

        $scope.personalAccounts = accounts;
        //console.log(typed, $scope.personalAccounts);
      }).
        error(function(data, status, headers, config) {
        console.log(data);
      });

    }

    $scope.otherAccount = "";

    $scope.updateOtherAccounts = function(typed) {
      $http({method: 'GET', url: 'http://localhost:4567/api/otheraccounts?query=' + typed}).
      success(function(data, status, headers, config) {
        var accounts = [];
        $.each(data.response.docs, function(i, val) {
          accounts.push(val.name + " " + val.bank_t + " " + val.account_t);
        });

        $scope.otherAccounts = accounts;
        //console.log(typed, $scope.otherAccounts);
      }).
        error(function(data, status, headers, config) {
        console.log(data);
      });

    }
   */

  });
